name: Welcome New Contributor

on:
  pull_request:
    types: [opened]

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Send welcome message
        run: |
          echo "Welcome to the repository! We're excited to have you as a contributor."
